services:
  haushaltsbuch-app:
    build: .
    container_name: haushaltsbuch-app
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      DATABASE_URL: file:/app/data/haushaltsbuch.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
